rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/performanceRecords/{performanceRecordId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
